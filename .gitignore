# Playwright CLI output (may contain credentials)
.playwright-cli/

# Impeccable review captures, question logs and hook cache
.impeccable/review/
.impeccable/questions/
.impeccable/hook.cache.json

# graphify knowledge graph output (would be published by GitHub Pages)
graphify-out/
